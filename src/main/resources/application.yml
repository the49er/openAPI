spring:
  datasource:
    url: jdbc:h2:./testDataBase
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    url: /api-docs.yml